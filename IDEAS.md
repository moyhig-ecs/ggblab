# ggblab — Integration & Feature Ideas (メモ)

以下は `ggblab` と Python を組み合わせて実現できる実用的アイデアのメモです。
短い説明と「次の一手」を付けています。

1. Auto-generate exercise pack (演習自動生成)
   - 概要: パラメータ化したシーンテンプレートから複数の `.ggb` を生成して配布用パックを作る。
   - 次の一手: `examples/` にテンプレートを1つ置き、`scripts/generate_exercises.py` を作る。
   - 優先度: 高

2. Automatic grading (自動採点 PoC)
   - 概要: 提出された `.ggb` をパーサで解析し、期待される構成（位置・依存関係）と照合してスコアを返す。
   - 次の一手: `ggblab_extra` のパーサを使って小さな採点ルールセットを作る（例: 点の存在／距離許容誤差）。
   - 優先度: 高

3. Symbolic verification & synthesis (SymPy 統合)
   - 概要: SymPy で導関数や式変形を行い、その結果を GeoGebra に反映して視覚的検証を行う。
   - 次の一手: `examples/sympy_geogebra_demo.ipynb` を拡張して "derive -> visualize" のワークフローを示す。
   - 優先度: 中

4. Batch image export / thumbnails (教材配布向け)
   - 概要: `getPNGBase64` を使って多数の `.ggb` のサムネイルやスライド用画像を自動生成。
   - 次の一手: ヘッドレスで動くスクリプト `scripts/batch_render.py` を作成して、ffmpeg 連携も検討。
   - 優先度: 中

5. Animation pipeline (動画生成)
   - 概要: パラメータ t を評価してフレーム毎に GeoGebra の状態を作成 → PNG を連番で保存 → ffmpeg で mp4 に合成。
   - 次の一手: `examples/animation_demo.ipynb` の PoC を作る（短いパラメトリック例）。
   - 優先度: 中
      - 備考: 将来的には `manim` スクリプトを自動生成して高品質なアニメーションを作る計画だが、これは中長期の作業として扱う。

6. Interactive widgets (教育用 UI)
   - 概要: `ipywidgets` と連携し、スライダーや入力フォームから GeoGebra を操作する教材インターフェースを作る。
   - 次の一手: 小さなウィジェットサンプルを `examples/widgets_demo.ipynb` に追加する。
   - 優先度: 低〜中
   - 備考: `ipywidgets` は将来的に、シングルカーネルから複数の GeoGebra Applet を同時に操作するユースケースが確認できたときに本格採用を検討します。

7. Data export for ML (特徴量生成)
   - 概要: `ConstructionIO.save_dataframe` で Parquet に保存し、GNN 等の入力に使えるデータセットを作る。
   - 次の一手: `examples/dataset_builder.py` を作り、1つの教科書チャプターを変換してみる。
   - 優先度: 中

8. Server-side rendering / API
   - 概要: サーバ上で `.ggb` を受け取り PNG を返す REST API（教材自動化や LMS 連携用）。
   - 次の一手: 軽量 FastAPI サービスの PoC を作成する。認証やリソース制限を検討。
   - 優先度: 低

9. Logging + replay (操作再生)
   - 概要: 学習研究向けに操作ログを記録して再生・解析する機能。ヒント生成や行動分析に有用。
   - 次の一手: フロントエンドのイベントを JSON で保存するスキーマを定義する。
   - 優先度: 低
   - 備考: 必要な研究者やプロジェクト要望があれば実装する（オンデマンド）。`EventListener` の基礎は既に含まれているため、最小限の追加で開始可能。

10. Parametric scene library (教材テンプレ化)
    - 概要: 再利用可能なシーンテンプレート群（章・節ごとのベース）を `examples/scenes/` に整備。
    - 次の一手: 教科書の Chapter 1 の 1-2 シーンをテンプレ化する。
    - 優先度: 高

---
運用メモ:
- 小さな PoC を短期間で回すと有用性が早く検証できる (例: サムネ生成 or SymPy→GeoGebra の導関数可視化)。
- 優先度は教育効果と実装コストを考慮して暫定付与しています。

もし欲しければ、これらのうち一つを選んで私が PoC を作成します。選ぶ場合は番号を教えてください。

---

## ブレインストーミング追記 (AI と共同で深掘りするための補助メモ)

以下は「短期間で価値を検証する」観点での追加アイデア、評価指標、実装の落としどころ、そして AI を使った実験パターンの提案です。

- **短期的なクイックウィン**: サムネイル一括生成、SymPy→可視化（導関数や交点の可視化）、単純な存在チェック採点ルール（点や線があるか）
   - 理由: 少ない実装コストで教材配布や最初の自動採点効果を確認できる。

- **AI を使った採点支援パターン**:
   - ルールベース + AI レビュー: まず厳密なルール（距離誤差、点の存在）で一次採点し、失敗例を GPT 系に要約して「改善提案」を出す。
   - 構造類似度 + スコア補正: グラフ編集距離(GED)や部分木マッチで構造スコアを出し、AI を使って意味的に等価な変形を検出（記号的等価判定を補完）。

- **評価指標**:
   - 精度（reference に対する合致率）、偽陽性率（誤って合格させる割合）、フィードバック有用性（人による主観評価）
   - 実行時間（1 件あたり秒）とスケーラビリティ（バッチ処理のスループット）

- **PoC 提案（短期→中期）**:
   - PoC A (短期): GED ベースの採点ノートブック
      - 目標: リファレンス `.ggb` と回答 `.ggb` をパースして NetworkX グラフ化、簡易 GED を計算してスコア化。差分ノード一覧を出力。
      - 成果物: `examples/poc_ged_grader.ipynb`（小さいサンプル付き）
   - PoC B (中期): ルール + 座標アライメント
      - 目標: 重要なオブジェクト（点 A,B, line l）に対し Procrustes 的に位置合わせして許容誤差評価、幾何的不変量（距離比・角度）で部分採点。
      - 成果物: スコアと詳細フィードバックを返すスクリプト
   - PoC C (探索): 学習ベース (GNN)
      - 目標: 代表的な正解/誤答ペアを少量作成し、グラフ類似度学習で高次元の類似性を学ばせる（より意味的等価性を捉える）

- **データ準備 & ラベリング**:
   - まずは手動で 10–30 ペアのリファレンス/誤答を用意して、評価とフィードバックの質を人が確認する。
   - `ConstructionIO.save_dataframe` を使って JSON/Parquet に特徴量を蓄積。

- **AI プロンプト戦略（補助）**:
   - 自動フィードバック生成プロンプト: 「学生の構成（要約）と期待する構成を与えて、3 点の改善アドバイスを出して」
   - カテゴリ分類プロンプト: 「この差分は『位置ずれ』『オブジェクト欠落』『構造誤り』のどれかに分類して」

- **リスクと緩和策**:
   - リスク: 学生の『等価変形』をルールで誤判定する。緩和: SymPy を使った式同値判定や AI によるセマンティックチェックを組合せる。
   - リスク: 大量提出で計算コストが高くなる。緩和: まず軽量ルールでスクリーニングし、詳細解析はサンプルに限定する。

- **次の一手（推奨）**:
 1. PoC A を `examples/` にノートブックで実装して動かす（短時間で目に見える成果が出る）。
 2. サンプル 5 件を用意して評価（人による妥当性チェック）
 3. 必要なら PoC B を作り、位置合わせ＋許容誤差で精度を上げる

---

気になるアイデアやまず試したい PoC があれば番号で教えてください。私が選択された項目の PoC 実装（ノートブック/スクリプト）を作成します。

---
（注）この追記は短時間での価値検証を主眼にしています。将来の拡張として、学習データ生成パイプライン、LMS 統合、認証付きサーバレンダリングなどを段階的に追加できます。
