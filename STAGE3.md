# replay / stage 3 (`%%ggb` magic) + stage 4 (Julia host) — 2026-09-10

## `%%ggb <host>` (ggblab/ipymagic.py)
- `%load_ext ggblab.ipymagic` → `%%ggb g [--plan] [--timeout S]`. Body → `parse_cell(dialect="python")` (28 heads, `:label` refs, unknown head = error) → `adapter.plan` (ONE `Eval` per batch, `:const :new` → `new`) → `adapter.apply(g, …)`. Result `_` = flat label list (`"t1,c,a,b"` from one Polygon is split). Host is an explicit argument — no `GeoGebra._instance` / `user_ns["ggb"]` lookup (the same coupling stage 2 removed from sympy).
- Acceptance (`examples/stage3_magic.ipynb`, gate #2 group g1 = lesson 04, 10 statements): `--plan` strings == gate #1 `v2` strings; after `:const :new` + resend the applet's `<construction>` is byte-identical to `probes/stage2/gate2/v2_g1.xml` (md5 14bab5f7).
- Shared JS fix (M0, `ggblab/host/mount.js`): `eval` now try/catches per command — entry = labels | null (GeoGebra refused) | `{error}` (Apps API threw, e.g. TriangleCenter's lazy "Discrete commands not loaded yet"); before, one exception lost the whole batch's labels.

## Julia host (julia/host/html_host.jl, module `GGBLabHost` = position marker; the name is the teacher's)
- Same mailbox (C0 RPC), same JavaScript (`ggblab/host/mount.js` read by both hosts), same 8 verbs; `Downloads` (stdlib) + `JSON`; `kernel_id` from IJulia's `connection_file`; `find_server` = JUPYTERHUB env or `jupyter --runtime-dir`'s `jpserver-*.json` probed with `/api/kernels/<id>`. `listen` / `events` / `wait_update` are pull-only (no task). No comm, no HTTP.jl.
- Contract: `tests/test_julia_host_parity.py` runs julia and asserts the request JSON of all 8 verbs equals Python's `to_json`, `mount_key` three forms, `_q` percent-encoding, one JS file.
- Acceptance (`examples/julia_host.ipynb`, kernel julia-1.12): 6-command `command` 0.29 s, `value("a") = 2`, `xml` 5839 chars, 6 add events, `listen` callback fires on pull, `wait_update("A")` woke after 13.04 s on a browser-side drag, `kind("c1") = circle`.
- Not done: `@ggb` macro → Construction (brace question is the teacher's), a Julia Construction parser, JupyterHub crossing.

Record = lancedb-rag `conversations/2026-09-10/RECORD_ggblab_replay_stage3_magic_stage4_julia_20260910.md`.

## stage 3, second slice (2026-09-11): eg10 / eg11 replayed on the v2 host; eg12 = closed-world material

- `examples/eg10_slider.ipynb` (v2): the v1 file `2025_06_08.ggb` (its own sliders `k`, `n_1`) by `xml_in`; layers from the XML DataFrame; the loop blocks on a pull (`events(wait=…)`) for `k` or `n_1`; `k` → `ShowLayer/HideLayer` + `SetValue(n_1, 0)` (one eval; our own `update:n_1` drained), `n_1` → `SetVisibleInView(obj, 1, bool)` for the objects of layer k. Measured (probes/stage3/eg10_v2_measure.json): 91 rows in 0.85 s; reactions 0.01 s (mailbox backlog from a run that had timed out — drain again when a cell is re-run) / 31.87 s (driver delay) / 6.73 s / 6.8 s (one operation, one reaction; driver gap 7 s).
- **Visibility bit, measured**: `SetVisibleInView` changes only the `<show ev=…>` bitmask (bit 1 set when hidden in Graphics 1; bit 2 set when shown in Graphics 2); `<show object=…>` (the DataFrame `ShowObject` column, C6) and `api.getVisible()` do not move. `api.setVisible` (Apps API, not in the 8 verbs = the teacher's item #9) moves `<show object>`. So the XML-side count in the notebook did not change after the `n_1` steps although Graphics 1 did. `HideLayer/ShowLayer` (eg8) move `<show object>`.
- `examples/eg11_construction.ipynb` (v2): the eg11 2D construction with explicit labels (25 statements → 30 labels, 0 errors); the v1 `getValueString` + `ggb.parser.tokenize` reads are retired — list members are named (`D = lst1(1)`) and read from the XML IR, the list length is `Length(...)` read by `value`. Drag of `C` (driver `SetCoords`): woke 6.6 s / 8.01 s; D, E follow (x = 0.5, y = ±0.866), and become NaN when C leaves the circle (Length(lst1) 2 → 1). probes/stage3/eg11_v2_measure.json.
- Parser: **v1 relative references** (`_`, `__`, `_N`) now raise `RelativeReference` (they parsed as bare labels and would have been sent literally). Positive control: eg12's magic cells = 17 lines; the textbook fixture (1,007 `@ggb` lines) has 0; stage-1 gate still PASS; tests 51. Grammar note (registered): an uppercase-initial label before `(` reads as a command head (`L1(1)` → UnknownHead) — list labels are lowercase in v2 cells.
- **eg12 (3D worksheet) = material for the teacher, not run**: probes/stage3/eg12_closed_world.json — of 89 magic-cell lines 77 parse; outside the 28 heads: `IntersectPath` 4, `Element` 4, `SetCoords` 2 (a scripting command, not a construction head), `Cylinder` 1, `Translate` 1 (eg11's blocks add IntersectPath 5 / Cylinder 2 / Translate 1); heads hidden inside `{…}` list definitions (brace question): `Tangent` 1 (eg11). The ruling is the teacher's: extend HEADS (3D solid / path intersection / list element / transformation each need a Signature), treat SetCoords as `:api`-class, and whether the closed world looks inside braces.

## stage 4, second slice (2026-09-11): the Julia entry = a string macro that delegates to the Python parser (ruling A + PythonCall)

- Survey first (teacher: v1's macro is confused and does not live with arithmetic): lancedb-rag `conversations/2026-09-11/SURVEY_julia_ggb_macro_v1_20260911.md` — the root is ORDER: Julia's parser reads the `@ggb` body before the macro runs (juxtaposition `u v` and `²` are parse errors; `C' = …` becomes a function definition; `0.6260` → `0.626`; `2π/5` → `(2π) / 5`; bare identifiers are evaluated; the textbook escaped with `:A` on 673 lines and `"…"` on 53). Numbers: 1,227 lines through Julia 1.12.5 (`probes/stage3/julia_*_sweep*`).
- Ruling (teacher 2026-09-11): **A — a string macro, the parser delegated to Python through PythonCall.** `julia/host/ggb_macro.jl` (module `GGBLabMacro`, position marker): `ggb"…"` → `ggblab.parse.parse_cell(text, dialect="python")` (PythonCall; `JULIA_PYTHONCALL_EXE` = the lab's conda python, `JULIA_CONDAPKG_BACKEND=Null`, ggblab on PYTHONPATH) → a Python `Construction`; `ggb"…"g` applies `ggblab.adapter.plan` to the host (`Eval` → `command`, `:const :new` → `new_construction`, `:const :undo` → `delete`, `:api` → `ClosedWorldError`); `to_ggb` / `labels` / `plan` / `flatten_labels`. Python parse errors (UnknownHead / RelativeReference) cross the boundary as `ClosedWorldError`. No `$` interpolation. C6 kept: neither the parser nor `geometry_ir` is reimplemented in Julia (`examples/julia_macro.ipynb` reads the IR through PythonCall).
- Tests: `tests/test_julia_macro_parity.py` (2) — `to_ggb` byte-identical to `parse_cell` for the gate-2 g1 body and for a body that v1 could not carry (`C' = l1(1)`, `O'' = Intersect(f, g)`, `0.6260`, `2π/5`, `(u v) / (v v)`, `²`); the closed world crosses the boundary. Suite 54.
- **Parser fix found by this replay**: `ggblab/parse.py` treated `'` as a string quote (`split_top` / `_balanced` / `strip_comment`), so `Polygon(:A, :C', :A')` split into two arguments and reached the applet as `Polygon(A, :C', :A')` ("Please check your input"); and a raw Definition kept its `:label` colons (`d = Distance(:A, :C)² + r`). Now only `"` delimits a string and `:label` inside a raw definition is dereferenced (`_deref`; `l: 2x + 3y = 4` keeps its colon). Gate #1 (render equivalence, 805 rows) unchanged: exact 635 / label 83 / ws 4 / paren 4 / num 4 / quote 1; stage-1 gate PASS.
- Measured (8899, kernel julia-1.12, Restart & Run All): 14-statement construction with prime labels, `2π/5`, `0.6260`, `x²` → 17 labels in **1.24 s**, errors []; `to_ggb` byte for byte; `value("d") = 0.626`, `Length(lst1) = 2`; IR through PythonCall: C' = (0.951, −0.309), A' = (−0.951, 0.309). Before the parser fix the same run had 2 refusals (t1, d) — the positive control of the fix.
