#!/usr/bin/env bash
# Install Python requirements for the binder build environment
set -euo pipefail

echo "Installing binder requirements..."
pip install --no-cache-dir -r binder/requirements.txt || true

echo "Ensuring jupyterlab is installed..."
pip install --no-cache-dir jupyterlab || true

echo "Binder postBuild finished."
